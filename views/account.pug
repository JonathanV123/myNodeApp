extends layout

block content
    h2= title
    form(action="/account" method="POST")
        label(for="name") Name
        input(type="text" name="name" value=user.name) 
        label(for="email") Name
        input(type="email" name="email" value=user.email) 
        input.button(type="submit" value="Update Account Information") 


